<%= form_with(url: admin_merchants_path, method: :post) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit "Submit", class: "bg-gray-300 text-gray-800 py-2 px-4 rounded hover:bg-gray-400" %>
<% end %>