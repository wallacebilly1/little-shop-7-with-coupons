<h1>Invoices</h1>

<div id="invoices", class="columns-2">
  <ul> <% @invoices.each do |invoice|%>
    <div id="invoice-<%= invoice.id %>">
      <li>Invoice <%= link_to "#{invoice.id}", merchant_invoice_path(@merchant, invoice) %></li>
    </div>
  <% end %> </ul>
</div>